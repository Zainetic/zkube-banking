# ---------------------------------------------------------
# ACCOUNTS SERVICE
# ---------------------------------------------------------
apiVersion: apps/v1
kind: Deployment
metadata:
  name: accounts-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: accounts
  template:
    metadata:
      labels:
        app: accounts
    spec:
      containers:
      - name: accounts
        image: zkube-accounts:latest
        imagePullPolicy: Never 
        ports:
        - containerPort: 8081
        livenessProbe:
          httpGet:
            path: /api/accounts/health
            port: 8081
          initialDelaySeconds: 5
        readinessProbe:
          httpGet:
            path: /api/accounts/health
            port: 8081
          initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: accounts-service
spec:
  type: LoadBalancer 
  selector:
    app: accounts
  ports:
    - port: 8081
      targetPort: 8081

---
# ---------------------------------------------------------
# TRANSACTIONS SERVICE
# ---------------------------------------------------------
apiVersion: apps/v1
kind: Deployment
metadata:
  name: transactions-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: transactions
  template:
    metadata:
      labels:
        app: transactions
    spec:
      containers:
      - name: transactions
        image: zkube-transactions:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8082
        livenessProbe:
          httpGet:
            path: /api/transactions/health 
            port: 8082
          initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: transactions-service
spec:
  type: LoadBalancer 
  selector:
    app: transactions
  ports:
    - port: 8082
      targetPort: 8082

---
# ---------------------------------------------------------
# NOTIFICATIONS SERVICE
# ---------------------------------------------------------
apiVersion: apps/v1
kind: Deployment
metadata:
  name: notifications-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notifications
  template:
    metadata:
      labels:
        app: notifications
    spec:
      containers:
      - name: notifications
        image: zkube-notifications:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8083
        livenessProbe:
          httpGet:
            path: /api/notifications/health
            port: 8083
          initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: notifications-service
spec:
  type: ClusterIP 
  selector:
    app: notifications
  ports:
    - port: 8083
      targetPort: 8083